pipeline {
    agent maven

    environment {
        PATH = "/opt/apache-maven-3.9.11/bin/mvn:$PATH"
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean deploy'
            }
        }
    }
}
